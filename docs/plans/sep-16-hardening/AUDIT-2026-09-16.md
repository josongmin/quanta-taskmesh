# Sep-16 hardening — 구현 직후 적대적 감사와 그 처리

- 날짜: 2026-09-16 (구현 완료 직후, 같은 날)
- 방식: 독립된 3-track 감사를 병렬로 실행 — (1) engine/runtime 정합성, (2) 증명 강도
  (test·mutation·model-check가 실제로 무엇을 증명하는가), (3) tooling/CI/deps/docs/계약.
  각 track은 코드를 수정하지 않고 반례를 재현했다.
- 결과: P0 3건, P1 12건, P2 ~30건, P3 다수. **모두 처리했다** — 코드 수정 + 실제 test,
  또는 "동작 유지"를 근거와 함께 명시. 아래 표가 finding → 처리 → 증거다.
- 계약 변경은 [ADR 0003](../../adr/0003-sep-16-hardening-contracts.md) D05/D08/D09/D10 개정과
  D13–D16에 기록했다.

## 처리 원칙

1. 짜치는 test 금지: 모든 새 test는 mutation(`tools/verification/mutations.json`)이나
   반례 재현으로 "이 test가 없으면 무엇이 통과하는가"를 보였다.
2. hang은 실패가 아니다: hang으로만 regression을 드러내던 test는 상한 + release-on-drop
   fixture로 assertion 실패가 되게 바꿨다.
3. 조용한 실패 금지: `()`를 반환하던 API, 무시되던 `bool`, 버려지던 deadline, 문자열로
   뭉개지던 실패는 모두 typed outcome이 됐다.

## Track 1 — engine / runtime

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| P1-1 | `promote`가 `select` 후 budget을 검사해 64번째 선택의 DRR/WFQ 비용이 dispatch 없이 남음; continuation gap에서 새 도착이 queue를 추월 | budget-before-select + 부작용 없는 `has_runnable`; in-class FIFO(`CapacityBlock::QueuedBehind`, HOL 예외는 다른 capability pool뿐) | `drr_order_is_exact_across_the_promotion_budget_boundary`, `a_newcomer_queues_behind_a_runnable_head_of_its_own_class` (waker 재진입); mutations `promotion-budget-charges-an-unmade-dispatch`, `newcomer-overtakes-a-runnable-head` |
| P2-1 | queued 경로는 budget 만료 후 promotion을 시작(D09 비대칭) | 대칭: 만료 후 promotion은 `return_unstarted`; timeout 시 마지막 조회는 engine의 이유 보고용 | `timeout_after_racing_promotion_unwinds_instead_of_starting_v1`; mutation `budget-ignored-on-a-racing-promotion` |
| P2-2 | requested-stack async panic: runtime teardown이 `Panicked` 응답보다 먼저 | runtime을 panic boundary 밖에서 생성·소유; 응답 후 teardown | `a_panic_answer_is_not_delayed_by_a_blocking_child_teardown` (bounded); mutation `panic-reply-waits-for-teardown` |
| P2-3 | `capabilities()` 미소비 | Track 2 P0-2와 함께 처리 | 아래 |
| P2-4 | 비-`CooperativeWithDeadline` 클래스의 deadline이 조용히 무시 | `GovernorError::DeadlineUnsupported` (breaking) | `deadline_on_a_class_that_cannot_enforce_it_is_refused_not_ignored`; mutation `unsupported-deadline-silently-dropped` |
| P2-5 | disabled fallback으로의 degrade가 validation 통과 | 구성 시점 거절 | `degrade_to_a_disabled_fallback_is_rejected`; mutation `degrade-to-disabled-fallback-accepted` |
| P2-6 | claim 후 미전진 permit이 reclaim 가능; `release`가 `()` | `claim`이 lease touch; `release -> ReleaseOutcome` (`#[must_use]`); host lease `advance -> Result` + `LeaseReclaimed` | `a_claim_restarts_the_lease_staleness_clock`, `a_lease_the_sweep_reclaimed_before_dispatch_refuses_to_advance_v1`; mutation `claim-does-not-touch-the-lease`; loom `claim_and_reap_of_a_stale_promotion_fail_closed_both_ways` |
| P2-7 | `reconcile_memory` epoch TOCTOU (두 lock) | 한 lock 안에서 epoch 배정 | `an_unordered_reconcile_takes_the_epoch_after_whatever_is_recorded`; loom `unordered_concurrent_reconciles_both_apply` |
| P3 | WFQ rebase 문서, `PermitLedger.released` dead field + `AlreadyReleased` 변형, `forget_terminal_ticket` O(N), `is_recursive` clone + lock 안 spec 검증, LargeStack hint 과금, D08 문구 | dead field/변형 제거; retention `O(log n)` + `retained_terminal_tickets()`; `precheck` lock 밖, recursion guard borrowed lookup; D08 문구 정정. LargeStack hint-only 과금은 **의도된 모델**(hint = 분류, 세 gate가 한 blocking executor를 공유 — `SubstrateHint` 문서)로 유지하고 `execution_plan.rs`에 근거를 적었다 | `observing_a_terminal_record_releases_its_retention_slot`; 기존 recursion/attribution test 유지 |
| — | 감사 중 남은 probe 파일 | 감사 종료 시 없음 확인 | `git status` |

## Track 2 — 증명 강도

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| P0-1 | loom/shuttle이 toy replica; H16-014/README/COVERAGE Q29 주장이 거짓 | `src/sync.rs` seam; 실제 `Governor`를 도는 loom 5 / shuttle 4 모델; checker는 optional feature (consumer lockfile 불변) | `just loom` (9–810 interleavings), `just shuttle` (10k schedules ×4); ADR D13 |
| P0-2 | `ExecutorCapabilities` 장식 | builder가 `declared_workers < cpu gate`를 typed 거절; Rayon/BlockingPool이 실제 프로필 선언; `executor_capabilities()` | `a_cpu_gate_wider_than_the_executor_declares_is_refused_at_build`, `the_cpu_gate_and_the_rayon_pool_are_sized_from_one_answer`, `the_adapter_declares_what_it_can_honestly_promise`; mutation `executor-declaration-ignored` |
| P1-1 | TM16-002/024/012 regression이 hang | 5초 상한 + `ReleaseOnDrop`; TM16-012는 count oracle + bounded thread | mutations `blocking-run-budget-not-applied`, `terminal-reply-waits-for-teardown`, `drr-walks-the-ring-visit-by-visit` (모두 assertion으로 KILLED) |
| P1-2 | 동어반복 test 2건 | 정확한 순서 assert; survivor는 ticket 순서로. (정정: `b,b,b,a,a,a`는 increment가 0으로 floor된 경우에도 나오는 순서라 그 assert 자체는 TM16-013을 잡지 못한다 — 잡는 것은 같은 test의 top-of-domain scale-invariance assert와 `weighted_share_survives_uniform_rescaling_of_every_weight`다) | `extreme_weights_still_produce_a_non_zero_increment`, `cancelling_from_the_middle_preserves_the_order_of_the_survivors`; mutation `wfq-scale-loses-precision` |
| P1-3 | TM16-010 mutation이 debug_assert에 죽음 | `profile: release` + 실제 assertion message; 2중 방어(claim의 permit 재검사)를 note에 명시 | mutation `dead-ticket-survives-unwind` |
| P1-4 | `expect_message` 6개 비어 있음, control 오표기, finding 22개 mutation 없음 | runner가 빈 message/오표기를 거부; 37 entries (Python 도구 5개 포함) | `just mutants-critical` 37/37 (2차 감사 후 43/43, 증명 강화 후 45/45, 3차 후 62/62) |
| P1-5 | bench-iai.sh test 없음 | PATH shim end-to-end 13 test | `tools/bench/tests/test_iai_gate.py` |
| P1-6 | D03 test 없음 | host test: 선언 클래스의 deadline 계약 유지 + ledger는 fallback 클래스 | `a_degraded_submission_keeps_its_declared_deadline_contract` |
| P2 | `in_use <= 2` → `== 1`; intake `Rejected(_)` → 정확 verdict; Accepted-phase cancel; spawn 실패 vs panic; gate-vs-executor; child-scope/foreign handle; alloc_probe reject 경로; H16-006-A06/test-count 문구 | 모두 반영 | `a_caller_that_leaves_while_the_job_is_accepted_but_not_started_keeps_it_charged`, `each_failure_mode_is_reported_as_itself` (WorkerUnavailable via `stack_size_bytes(u64::MAX)`), `hardening_child_scope.rs`, `test_the_real_producer_refuses_to_count_a_rejected_admission_as_an_op` |
| P2 | `apply_memory_delta` saturating clip | 음수 underflow는 sticky accounting fault (방어적·미도달; `capacity_for`의 `AccountingFault`와 같은 지위) | code + 주석 |
| P2 | e2e_chaos `max_live_seen` sampled | 유지 (샘플링 관찰은 상한 검증에 충분; 정확한 peak는 engine gauge가 담당) | — |

## Track 3 — tooling / CI / deps / docs

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| P0-1 | `iai-callgrind-runner --version` 없음 → script가 무진단 사망; Linux gate 영원히 red | `tools/bench/iai_gate.py` (install-list / self-report), 모든 exit에 진단, stamp는 성공 후 | 13 shell tests; mutation `iai-runner-self-report-ignored` |
| P1-1 | cache key ≠ fingerprint → 영구 BASELINE_CREATED; PR이 main과 비교 안 됨; job이 status로 실패 안 함 | key = `bench-iai.sh fingerprint`(ref 없음); 명시적 verdict step; PR에서 입력 불변인데 baseline 없으면 warning | `.github/workflows/bench.yml` |
| P1-2 | `just proof`가 required 4개 누락 | `just matrix` + proof parity 검증(validator) | `test_the_real_proof_recipe_expands_to_exactly_the_required_set`; mutation `proof-parity-not-checked` |
| P1-3 | receipt validate가 self-report 신뢰; `--tier` append 버그 | HEAD/dirty 재도출 + sub-receipt 일관성; attestation 경계 문서화; `--tier` 수정 | `test_the_live_validator_rejects_a_forged_clean_receipt_on_a_dirty_tree`; mutation `receipt-validator-trusts-the-receipts-dirty-flag` |
| P1-4 | semgrep test 규칙이 `#[tokio::test]` 무시; `println!($...)` dead; 5/38만 test | async-aware; 38/38 fire+clean fixture + coverage test; 실제 finding 25건 수정 | `tools/semgrep/tests/test_rules_fire.py` |
| P2-1 | receipt test가 live tree에 probe 파일 기록 → gate 비-hermetic | 임시 git repo fixture | `test_receipt.py::repo` |
| P2-2 | arch checker가 `_tests.rs`/`src/tests/` 건너뜀, fq path 미검출, phantom `taskmesh-core` | `src/**` 전체, 세 spelling, `#[cfg(test)]` gating 검증, phantom 제거 | 6 tests; mutation `arch-checker-trusts-the-test-module-naming-convention` |
| P2-3 | production 규칙이 `_tests.rs` 제외; non-git export | boundary 규칙은 `src/**` 전체 + fq path regex; non-git export는 기존 target-set test가 loud하게 실패 | rule pack + README |
| P2-4 | alloc threshold 4 vs 측정 3; 계측 self-check 없음 | threshold = 3; `counter_check` 필드(schema 2); reject 경로 real-producer test | `test_the_threshold_is_the_measured_baseline_not_a_cushion`; mutation `alloc-gate-trusts-a-broken-counter` |
| P2-5 | validate_inventory가 single-line `run:`만 인식 | YAML parse + 모든 shell 형태 | `test_workflow_invocations_are_read_from_every_run_shape` |
| P2-6 | `verify_clean_tree` 무검증; timeout fixture 없음 | `--require-clean`(CI) + dirty 기록; timeout/restore test | `test_a_timeout_is_invalid_and_the_file_is_still_restored` |
| P2-7 | stamp를 bench 전에 기록 | 성공 후 기록, 실패 시 fresh baseline 제거 | `test_a_failed_benchmark_leaves_no_stamp_behind` |
| P2-8 | deny ignore scope 주석 | graph-global임을 명시, 경계는 arch checker가 담당 | `config/deny.toml` |
| P2-9 | receipt stale | 재수집 (`receipts/`) | 아래 |
| P2-10 | pm-lint 공허; README `sources/**` | 유지(정직하게 문서화된 빈 집합 + 도구 자체 test); README 문구 수정 | — |
| P2-11 | 문서 drift 5건 | 모두 수정 (`runtime-inventory-baseline`, semgrep README/messages, VERIFICATION.md, release-checklist) | — |
| P2-12 | ICE dump tracked | `git rm` (3 June dumps); 2차 감사가 발견한 9-16 dump도 제거, `.gitignore`에 `rustc-ice-*.txt` 추가 | — |

## 2차 감사 (같은 처리 위에서, 2-track: 코드/주장 · 증명 표면)

1차 처리를 다시 적대적으로 읽었다. P0 0, P1 4, P2 8, P3 다수. 전부 처리.

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| T1-P1-A | `await_promotion` *loop* 경로의 budget 검사가 미검증 — `return Ok(permit_id)`로 바꿔도 host 130 test 통과. `biased` select에서 waker가 timer보다 이기므로 이 경로가 늦은 promotion의 실제 시작 경로 | 테스트 추가 (promotion이 먼저 landed, 만료된 budget으로 첫 poll) | `a_promotion_found_on_wake_after_the_budget_is_spent_is_unwound_v1`; mutation `budget-ignored-on-a-promotion-found-on-wake` |
| T1-P1-B / T2-N1 | D08 capability 예외(다른 pool에 막힌 head는 newcomer를 잡지 않음)에 test 0개 — `queued_head_blocks`를 항상 `true`로 바꿔도 367 통과 | `admit_resolved`로 large_stack(limit 1) head + blocking newcomer test | `a_head_blocked_on_another_capability_pool_does_not_hold_back_a_newcomer`; mutation `capability-blocked-head-holds-back-every-newcomer` |
| T1-P2-A | continuation gap에서 *다른 클래스* newcomer가 earlier arrival을 추월 | `GovernedState::promotion_pending` (모든 pass 끝에 갱신) — pending 중 queue 가능하고 자기 queue가 빈 클래스의 직접 admit은 `QueuedBehind`로 queue되어 continuation pass가 tier discipline으로 순서 결정; 자기 queue가 있으면 head 규칙(runnable이면 뒤에, 다른 pool에 막혔으면 추월)이 gap 안에서도 유지; queue 불가 클래스만 admit (D08 개정 문구 정정) | `a_newcomer_of_another_class_also_waits_for_the_pending_continuation`, `the_capability_exception_holds_inside_the_continuation_gap_too`; mutations `cross-class-newcomer-takes-the-continuation-gap`, `newcomer-overtakes-a-runnable-head` (재조준) |
| T1-P2-B | `Reject` overflow + memory `Queue` 클래스에서 `QueuedBehind`가 `CpuSaturated`로 shed (cpu/memory 모두 free); 주석 "non-queueing 클래스엔 queue가 없다"가 거짓 | `QueuedBehind`는 overflow 정책과 무관하게 `enqueue_or_full`로 (queue가 존재한다는 뜻이므로) | `a_reject_class_that_queues_on_memory_joins_its_queue_behind_a_runnable_head`; mutation `queued-behind-shed-by-overflow-policy` |
| T1-P2-C | `run_blocking` + `stack_size_bytes`가 `DedicatedStackThread`로 분류되어 `CompleteBy`를 수락(spec 위반; custody는 유지됐음) | `DispatchKind::DedicatedStackRuntime`(async) 분리; sync dedicated thread는 absolute deadline 거절 | `absolute_deadline_rejects_non_cancellable_blocking_work_v1` (stack 케이스 추가); mutation `absolute-deadline-accepted-on-a-blocking-stack-thread` |
| T1-P3 | builder `try_from` 실패 arm이 "more"를 "fewer"로; boundary test 없음(T2-N3) | `u64` 비교; `declared = gate-1` 거절 / `== gate` 수락 | mutation `executor-gate-boundary-off-by-one` |
| T1-P3 | `claim`의 도달 불가 분지가 답한 뒤 retention slot을 남김 | record 제거 (답을 지금 주므로 retain할 것 없음) | code |
| T1-P3 | `precheck` lock 안 중복; D06 불일치(`*_total`이 bare number); `ports.rs` 문서 경로; ICE dump(9-16) + ignore 규칙 없음; library-spec의 D05/D14 누락; 문구 과장 4곳(D08 "waker로만", D14 "모두 typed", D15 precheck, AUDIT P1-2 근거) | 모두 수정; `*_total`은 `exact_units` 직렬화 | code + docs |
| T1-P3 / T2-N2 | `ExecutionLease` disarm은 관찰 불가(등가 mutant) | test 주석과 ADR D14 문구를 "방어적"으로 정정 | — |
| T2-P1-1 | `--features loom` without `--cfg loom` → 모델 파일이 0 test로 컴파일되어 rail green | seam에 역방향 `compile_error!` | `sync.rs` |
| T2-P1-2 | gates-inventory parity가 *호출* 존재만 검사 — `continue-on-error`/`if:`/`\|\| true`/`set +e`/`exit 0`이면 집행 안 됨 | validator가 inventory gate를 호출하는 step의 그 형태들을 거절 | `test_a_gate_step_that_cannot_fail_the_job_is_reported` |
| T2-P2-1 (N5) | fingerprint가 4개 input 전부에 의존한다는 증명 없음(`[:1]` mutant 생존) | 각 configured input을 바꿔 fingerprint가 움직이는지 `main()`으로 test | `test_every_configured_fingerprint_input_changes_the_fingerprint` |
| T2-P2-2 | runner의 `WRONG_REASON` needle을 전체 출력에서 검색 — 다른 test의 실패가 이유를 대신 채움 | named test의 자기 출력 블록(libtest `---- name stdout ----`, pytest `___ name ___`/`FAILED …::name`)으로 범위 제한 | `test_the_reason_must_come_from_the_named_test_not_a_neighbour`, pytest 동일 |
| T2-P2-3 | 문서의 "19건" | 43으로 정정 (release-checklist, COVERAGE Q32) | — |
| T2-P3 | pytest 요약 `(0:01:20)` suffix → 0 collected → INVALID; ERROR된 named test → SURVIVED; `.pyc` mtime 재사용 | regex 파싱; `-rfE` + ERROR 라인을 실패로; `PYTHONDONTWRITEBYTECODE` | `test_pytest_long_runs_and_fixture_errors_are_classified` |
| T2-P3 | `run.py`에 timeout 없음(hang이 receipt 없이 영원히) | 1h timeout → FAIL 기록 | `run.py` |
| T2-P3 | `bench-iai.sh`: valgrind가 stdout에 아무것도 안 찍어도 fingerprint 계산; `-rc.1`을 release로 수락; 잉여 positional arg가 `cargo bench` filter로 전달돼 아무것도 측정 안 하고 QUALIFIED | 셋 다 fail-closed(빈 valgrind/rustc 거절, 정확한 버전, 인자는 없음 또는 `fingerprint`만) | `test_a_valgrind_that_prints_nothing_on_stdout_fails_closed`, `test_stray_arguments_are_refused_instead_of_filtering_the_benchmark`, `test_a_prerelease_runner_is_not_collapsed_to_its_release_version` |
| T2-P3 | bench.yml fingerprint step이 script exit를 삼킴; `git diff` on bad sha | `value="$(…)"` 먼저 대입 + 빈 값 거절; base sha 존재 확인 | bench.yml |
| T2-P3 | `test-architecture`/`gates-inventory`가 system `python3`(PyYAML 없으면 red) | `uv run python`; prerequisites 갱신; `pm-lint` 공허함을 inventory note로 명시 | Justfile, inventory.json |
| T2-P3 | receipt: mutation gate 2회 실행; gitignored 파일이 digest에 포함; 위조 receipt가 QUALIFIED로 출력 | gate `--skip mutants-critical` + sub-receipt에서 유도; `git ls-files --cached --others --exclude-standard` 기반 digest; `collect` shape 필수 키 + "gate results attested by collect" 문구 | `test_gitignored_files_are_not_part_of_the_source_identity`, `test_a_receipt_missing_the_collectors_shape_is_not_qualified` |
| T2-P3 | 문서 숫자 drift (380+, (31), 115) ; shuttle 헤더의 "promotion budget" 주장 | 정정 | — |
| T2-P3 (유지) | semgrep/arch regex가 trailing `// … tokio::`나 문자열 리터럴에 FP 가능 (tree는 clean) | FP-only; 규칙은 loud한 쪽으로 남긴다 | — |

## 3차 (breaking change 허용 후: release · 객관 감사 · 증명 상향 · 적대 감사 3 · TSan)

2차 처리 위에서 "breaking change 가능"이 승인되어 6-phase 계획을 실행했다. 모든 작업은
branch `hardening/sep-16`에 commit되어 있다 (`7d0504d` → `15121ff`).

### 3-1 Release 0.2.0 + semver 감사 (`39bee68`)

| 항목 | 처리 | 증거 |
|---|---|---|
| version | workspace 0.1.0 → 0.2.0; root `Cargo.lock`(stable)과 consumer fixture lock(1.81) 모두 targeted `cargo update -p`로 갱신 | `cargo metadata --locked` 양쪽 성공 |
| `cargo-semver-checks` 0.50.0 (`--baseline-rev 9ae9547 --release-type minor`) | contract 1 failing check(9 added fields on constructible structs), engine 4(`LeakSweepReport.retained_active`, `PolicySet` private fields·removed pub `substrates`), taskmesh 0, rayon 0. 도구는 return type·field width·wire·behaviour 변경을 보지 못하므로 pub-item diff를 수동으로 대조해 CHANGELOG cross-check 표에 실었다 | `CHANGELOG.md` "Breaking changes and migration" |
| migration fixture | `tools/consumer-msrv`는 이제 모든 migrated shape를 *실행*하는 binary (ClaimOutcome/ReleaseOutcome/Snapshot v2 보존식/GovernorError arms/custom CpuExecutor 선언·거절/DeadlineUnsupported/WorkerPanicked/JobAbandoned/TopologyConfig::validate/StageReleaseOutcome/ReconcileOutcome); `check.py`는 type-check가 아니라 `cargo run` + 마지막 ok line 요구 | `just consumer-msrv` PASS (default·rayon, 1.81) |

### 3-2 실질 qualification rail (`1dc1ff8`)

| 항목 | 처리 | 증거 |
|---|---|---|
| ThreadSanitizer | `tools/tsan/run.sh` + `just tsan` (nightly `-Zbuild-std`); nightly+rust-src 없으면 NOT_RUN(exit 2), 절대 PASS 아님 | `taskmesh-tsan status=CLEAN target=aarch64-apple-darwin` |
| coverage | `tools/coverage/report.sh` + `just coverage-report` (cargo-llvm-cov; report이지 threshold 아님) | lines 89.8 / regions 89.0 / functions 91.6 (receipt) |
| CI | `tsan`, `coverage`(artifact), `qualification`(clean checkout에서 `receipt.py collect`, QUALIFIED 아니면 red) job; inventory 22 gates / 22 required; `just proof` 체인에 tsan·coverage-report 포함; `run.py --skip` | `.github/workflows/ci.yml`, `tools/gates/inventory.json` |
| 첫 TSan run의 발견 | `stack_size_bytes(u64::MAX)`가 "OS가 거절한다"에 의존 — debug-built std에서는 spawn 안에서 overflow panic. host가 `MAX_REQUESTED_STACK_BYTES`(16 GiB) 초과를 결정적으로 거절 | `each_failure_mode_is_reported_as_itself`, ADR D16 |

### 3-3 객관 감사: `cargo mutants` 전수 sweep (`6a113a6`)

hand-written inventory의 대조군으로 도구가 생성한 mutant 전수를 돌렸다 (`7d0504d` export; 병합
트리에서 `-- --tests -- --test-threads=1`, `--timeout 300`). 생존자 전원을 triage했다.

| crate | mutants | caught | missed | unviable | timeout |
|---|---|---|---|---|---|
| taskmesh-engine | 416 | 313 | 58 | 43 | 2 |
| taskmesh (host) | 109 | 49 | 15 | 33 | 12 |
| taskmesh-contract pass 1 (자기 test만) | 162 | 50 | 81 | 27 | 4 |
| taskmesh-contract pass 2 (`--iterate`, engine·host test 포함) | 79 | 50 | 27 | — | 2 |
| taskmesh-rayon | 8 | 3 | 0 | 5 | 0 |

triage 분류와 처리:

| 분류 | 의미 | 처리 |
|---|---|---|
| HIGH GAP (13) | 계약(ADR/README)이 말하는 행동인데 어떤 test도 거절 못 함 | 각각 test 추가 + `mutations.json` entry(`finding: R3-mutants`, 13건). 예: stage release/leak reap이 memory-queued 작업을 promote; `per_request_memory_units` cap 미검증(cpu 쌍둥이만 있었음); AuthorityOnly-only pool의 capability limit 수락; full queue가 blocking cause(`SubstrateSaturated`/`MemorySaturated`) 대신 `QueueFull`; gap 안 unqueueable class가 admit 대신 queue로; DRR 산술 5개(모든 fixture가 cost 1이라 `visits_needed`가 항상 1) → heterogeneous-cost reference walk; `drr_ring_visits` 상수 oracle(`visits <= 2`만 검사); host `absolute_acquire_error_v1` 양방향(queued 중 만료 → `DeadlineExceeded`, 먼 deadline 아래 acquire timeout → `PermitAcquireTimedOut`) |
| LOW GAP | 진단 값·accessor·경계(off-by-one) | test 추가 (accessor 값, `MAX_TERMINAL_TICKETS` 정확 경계, staleness `==` 경계, interned capability name, child upward reconcile root attribution, cpu_auto gate == detected parallelism, `SlotCountTooLarge { pool: "cpu" }`, exact `Display` of every error/verdict variant) |
| 생산 코드 변경 (D03) | `degrade_allowed` guard mutant가 multi-hop degrade chain을 허용하고 cycle에서 무한 재귀 — 계약에 "one hop"이 명시돼 있지 않았음 | chained `DegradeToLight`(a→b→c, a→b→a)는 construction에서 거절; ADR D03·README 7·CHANGELOG |
| TIMEOUT = infinite loop | `granted >= PROMOTION_BUDGET → <`, `drain_effects` `!more`, waker `notified -> ()` 등: mutant가 무한 루프 → 사실상 kill | 기록만 |
| TIMEOUT but killed in substance (host 7) | 다른 test가 먼저 FAILED한 뒤 hang-only test(`while !poll_started { yield }`, `pending().await`)가 binary를 멈춤 | hang-only test 3개에 5 s timeout + named panic → clean CAUGHT |
| EQUIVALENT | `TicketGuard::disarm`(ticket id 재사용 없음), `SubmitOptions::unbounded`(`= default()`), `quantum * QUANTUM_STEP_MS`(상수 1), builder의 도달 불가 pool arm(validate가 먼저 거절), `>=` promote 변형(idempotent 추가 pass) | 근거 기록 |
| ACCEPTED | `accounting_fault` 도달 불가(u128 aggregates·u32 per-request), `assert_consistent`(debug-only oracle), `sync.rs` loom/shuttle 모듈(plain `cargo test`에서 미컴파일), `rebase_virtual_time`(u128 overflow는 2^32 max-cost enqueue 필요) | 근거 기록 |
| FALSE KILL (3) | load-sensitive test(`inflight_cap_is_never_exceeded_under_contention`, `cancellation_chaos_never_leaks`)가 mutant와 무관하게 실패해 CAUGHT로 오분류 | 진짜 상태로 재분류(1 ACCEPTED, 1 LOW GAP → test, 1 EQUIVALENT) |

새 test는 모두 mutant를 손으로 적용해 named assertion이 죽는 것을 보고 복원했다 (짜치는
test 금지). 마무리 sweep(최종 commit `15121ff` export, 동일 harness)의 결과는 아래 "3-7"에.

### 3-4 증명 상향 (`fe38e64`, `aa24e35`)

| 항목 | 처리 | 증거 |
|---|---|---|
| D08 gap을 *동시성* 아래서 | shuttle `check_random` 모델 2개(5,000 schedules; newcomer가 gap 안에 떨어지는 비율 ≈49%를 coverage counter로 단정 — vacuous run은 loud fail): same-class/empty-queue/capability-blocked newcomer는 항상 queue(gap 안 `QueuedBehind`)·예외만 admit·원래 도착 순서 보장·strand 없음·`promotion_pending` clear; Reject class만 overtake하며 정확히 tail 하나를 밀어냄 | `shuttle_governance.rs` (총 6 모델), mutations `sticky-promotion-pending`, `unqueueable-newcomer-queued-in-the-gap` (runner `cargo_args`+`env`로 `#![cfg(shuttle)]` target 지원) |
| admission 계약의 실행 가능한 명세 | `differential_model.rs`: 1–3 FIFO class·depth/reject·cpu cost/budget·capability pool 2개의 참조 모델을 proptest로 engine과 같은 무작위 op 시퀀스에 태워 매 step 모든 verdict·gauge·ticket 상태·live permit·terminal record 동치 (id bijection까지) | 11 mutant kill; rule 1 제거는 설계상 single-thread에서 관측 불가 → shuttle 담당 |
| facade | `SNAPSHOT_SCHEMA_VERSION`, `ResourceConversionError` 재export; MSRV fixture가 facade로 schema 단정 | `tools/consumer-msrv` |

### 3-5 적대 감사 3A — 외부 소비자 시선 (`b5eddd6`)

public docs만 보고 붙는 소비자의 관점으로 다시 읽었다.

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| 3A-P1 | `Snapshot::capabilities` key set이 점유에 따라 flap(빈 pool이 사라짐) → 대시보드가 key 부재를 0과 구분 못 함 | 등록된 모든 pool을 항상 나열(ungated는 limit 0) | `hardening_consumer_surface.rs` |
| 3A-P1 | README §2/§6/§7·interface 예제가 컴파일 안 되거나 모든 요청을 거절(§6이 deadline을 주는 class를 §2가 `CooperativeWithDeadline`으로 선언 안 함; `DeadlineUnsupported` arm 없음; `RayonCpuExecutor` 경로 없음) | 예제 수정 | README, `docs/taskmesh-external-interface.md` |
| 3A-P2 | facade에서 `MAX_CAPABILITY_SLOTS`, `RequestKey`, `TokioPermitWaker`, `RayonCpuExecutor`에 닿을 수 없음; `RequestKey`를 읽을 수 없음 | `taskmesh::{MAX_CAPABILITY_SLOTS, ext::{RequestKey(as_str, Display), TokioPermitWaker, RayonCpuExecutor(rayon)}}` | 동일 |
| 3A-P2 | `AdmissionDecision`/`ClaimOutcome`/`StageReleaseOutcome`/`ReconcileOutcome`/`advance_phase`를 버리면 permit leak인데 경고 없음 | `#[must_use]`; fuzz/bench에서 버리던 자리를 단정으로 | code |
| 3A-P3 | `TokioRuntime`/`Builder`/`Governor` Debug 없음(guarded state 노출 없이); `SubmissionDeadline` 비교 불가 | 수동 Debug; `PartialEq/Eq` | code |
| 3A-P3 | rustdoc stale(`SubmitOptions`, `SubstratePoolTimedOut`); CHANGELOG 누락(waker loop는 실제 adapter+`admit_waitable`, per-type `non_exhaustive`, `run_blocking` caller-wait cancel, 16 GiB cap, WFQ literal, blocking-pool 0.1.0 문자열) | 모두 수정 | docs |

### 3-6 적대 감사 3B — 증명 표면/CI (`453dfc5`)

| # | Finding | 처리 | 증거 |
|---|---|---|---|
| 3B-P1 | `receipt.py collect`를 repo root에서 돌리면 자기 출력(`receipt.json`)이 untracked → dirty → CI `qualification` job이 첫 run부터 red | `.gitignore`(`/receipt*.json`, `*.proptest-regressions`); gate/mutation/MSRV runner를 stub한 end-to-end test | `test_collect_qualifies_on_a_clean_tree_with_receipts_written_at_the_root` |
| 3B-P1 | `bench-iai`가 BASELINE_CREATED(비교 0건)로도 gate PASS | inventory `status_line: {marker, require}` — 자기 machine-readable line에 토큰이 있어야 PASS, 아니면 NOT_RUN | `run.py`, `test_run.py` |
| 3B-P2 | TSan 목록이 single-threaded lifecycle binary를 포함하고 thread-heavy binary(lease custody·dedicated stack·concurrent submitter·cancel-leak·in-crate TicketGuard·rayon adapter)는 빠짐 | 목록 교체; CI `vm.mmap_rnd_bits=28` | `tools/tsan/run.sh` |
| 3B-P2 | `class_can_queue`의 memory-`Queue` 절에 test 없음 | test + entry | `class-can-queue-ignores-the-memory-queue` |
| 3B-P3 | differential-only killer(abandon of a queued ticket must promote)가 256 cases에서 ~1/3 run 생존 | 기본 1,024 cases; entry는 `PROPTEST_CASES=2048` (5/5 kill) | `abandoning-a-queued-ticket-never-promotes` |
| 3B-P3 | shuttle gap 모델의 in-gap coverage에 floor 없음 | `MIN_IN_GAP = GAP_SCHEDULES / 20` | `shuttle_governance.rs` |
| 3B-P3 | 문서: `pending_block_reason`이 intake-time reason임을 미명시; D08 예외의 head-of-line 결과 미기록; library spec에 verdict 표 없음; "shuttle 4" | 모두 수정 | docs |

### 3-7 마무리: 전체 TSan · 최종 객관 sweep (`15121ff`)

| 항목 | 결과 |
|---|---|
| `just tsan` (교체된 넓은 목록) | 첫 run에서 `requested_stack_absolute_deadline_holds_permit_during_long_poll_v1`이 `inflight` 1≠0으로 실패. 원인은 race가 아니라 test의 시간 가정: D10은 deadline 답을 owned runtime teardown *전에* 보내고 lease는 teardown과 함께 돌아온다. 답 직후 `inflight == 0` 단정은 성질이 아니라 경주. 두 test(deadline·cancel 형제)를 `assert_drains`로 교체 — drain budget 안에 permit이 돌아오거나 loud하게 실패. 이후 `status=CLEAN` |
| receipt 검토 (R3C-1) | 재수집한 receipt에서 coverage 수치와 TSan target이 **없었다**: gate 결과는 stdout+stderr의 마지막 2,000자만 보존하는데 cargo의 stderr가 self-report line을 밀어냈다 (ADR는 "수치는 receipt"라고 약속). 3B-P1의 `status_line` 메커니즘을 self-report하는 모든 recipe로 일반화: `tsan`(`status=CLEAN`)·`coverage-report`(`status=REPORTED`)·`consumer-msrv`(`status=PASS`)가 inventory에 `status_line`을 선언하고, runner는 recipe의 *마지막* marker line을 verdict로 삼아 그대로 receipt에 남긴다(consumer-msrv는 surface별 line 뒤에 summary를 찍으므로 첫 line은 verdict가 아님). validator는 양방향으로 닫는다 — script가 `taskmesh-… status=`를 찍는데 `status_line`이 없으면(exit 0만으로 PASS, verdict line 유실) 거절, 어떤 script도 찍지 않는 marker면(모든 run이 NOT_RUN) 거절, 형식 오류 거절. mutations `self-reporting-gate-passes-on-exit-zero-alone`, `an-earlier-status-line-stands-in-for-the-verdict` (62/62) |
| 문서 컴파일 (R3C-2) | 마무리 검증에서: README 9블록·interface 문서 2블록·CHANGELOG 11블록의 rust 예제가 **어디서도 컴파일되지 않았다** — 3A가 손으로 고친 "컴파일 안 되는 예제"가 다시 썩는 걸 막는 장치가 없었다. `tools/doc-examples`(workspace member, `publish = false`): build.rs가 README·interface 문서의 ```rust 블록을 그대로 추출해 placeholder prelude(`Doc`·`ParseError`·`MyError`·`load()`·`spec`·`job`) 안의 async scaffold에 넣어 `cargo test --workspace`가 type-check한다. Markdown은 손대지 않는다(GitHub에 `# ` hidden line이 보이는 doctest 방식 배제). 검증: README의 `GovernorError::Cancelled`를 `Canceled`로 바꾸면 빌드 실패(E0599); fence 인식을 깨면 build.rs가 "extraction is broken"으로 panic; SOURCES에서 문서 하나를 빼면 test가 자기 목록으로 잡는다(첫 시도에선 test가 build.rs의 목록을 그대로 써서 이걸 놓쳤음 — 목록을 독립시켰다). CHANGELOG의 before/after 혼합 블록은 의도상 옛 API를 포함하므로 제외; migrated shape는 `tools/consumer-msrv`가 *실행*으로 덮는다. arch checker는 새 crate의 boundary 미선언을 fail-closed로 잡았고(`taskmesh`+`tokio`만 허용으로 선언) |
| 최종 객관 sweep (delta) | 전수 재sweep은 외부 부하(load avg 60–77, mutant당 60–115 s)로 중단하고, `15121ff` export에서 **7d0504d sweep 이후 바뀐 production 라인 위의 모든 mutant** + 중단된 run의 생존자(accessor·builder)를 대상으로 돌렸다 (`-j 2`, `--timeout 600`, 동일 harness). engine 15: 8 caught / 1 unviable / 6 missed — `capability_limit → 0/1`(호출자 0 → `the_readable_limit_is_the_limit_admission_enforces_and_zero_means_ungated` 추가, 두 mutant 손으로 kill 확인), `RequestKey::as_str`×2·`Display`·`Governor` Debug(facade 계약; host test를 세는 `--test-package taskmesh` 재실행에서 **4/4 caught**). host 14: 7 caught / 1 unviable / 1 timeout / 5 missed — timeout은 `1 << 34 → >>`(stack cap 0): named failure 2건 뒤 `requested_stack_async_runtime_honors_cooperative_cancel_v1`의 무한 spin → `bounded()`로 교체(`87cc459`), 같은 mutant가 이제 10 s 안에 named failure 4건; missed는 `builtin_pool_name` arm 4개(EQUIVALENT — `validate()`가 먼저 거절해 도달 불가)와 rayon-only `default_cpu_executor`(default feature harness에서 미컴파일). **방법론 주의**: 첫 시도에서 `CARGO_TARGET_DIR`을 `-j 3` job이 공유해 다른 job의 binary로 test가 돌아 거짓 MISSED(`capability_limits → {"xyzzy":1}`)가 나왔다; 공유 target 없이 재실행하면 CAUGHT. sweep은 항상 cargo-mutants의 per-job copy에 맡긴다 |

## 재수집 결과

`docs/plans/sep-16-hardening/receipts/local-2026-09-18.json` (+ `.gates.json`, `.mutations.json`):
branch `hardening/sep-16`의 **clean committed tree**에서 `receipt.py collect`로 수집
(root의 `receipt*.json`은 ignore되므로 수집 자체가 tree를 더럽히지 않는다; 전후 digest 일치).
22 gates 중 21 PASS — fmt/clippy/test/deny/semgrep/arch/py-lint/py-test/pm-lint/bench-gate/
gates-inventory, test-rayon/doctest/rustdoc/bench-smoke/consumer-msrv(1.81 default·rayon PASS),
loom/shuttle/tsan(`status=CLEAN target=aarch64-apple-darwin`)/coverage-report(`status=REPORTED
lines=93.85 regions=93.53 functions=95.76`; report이지 threshold 아님)/mutants-critical(62/62,
problems 0, dirty_tree false). 남는 NOT_QUALIFIED 사유는 **하나**: `bench-iai` SKIPPED_PLATFORM
(Linux/valgrind 전용). 이전의 "dirty tree" 사유는 commit으로 사라졌다. CI의 `qualification` job이
Linux clean checkout에서 같은 collector를 돌려 receipt of record를 남기며, `bench-iai`는 첫 run에서
BASELINE_CREATED(NOT_RUN)·두 번째 run부터 QUALIFIED다. 이 저장소에서 push는 접근 권한이 있는
credential로 사용자가 수행해야 한다(이 세션의 credential은 403).
